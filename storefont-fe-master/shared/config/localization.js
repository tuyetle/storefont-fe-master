export const { DATE_SHORT_TYPE } = require('./localization.es5');
export const { DATE_LONG_TYPE } = require('./localization.es5');
export const { PRICE_FORMAT_TYPE } = require('./localization.es5');
export const { NUMBER_FORMAT_TYPE } = require('./localization.es5');
export const { MONEY_FORMAT_TYPE } = require('./localization.es5');

const { CONFIG } = require('./localization.es5');

export default CONFIG;
